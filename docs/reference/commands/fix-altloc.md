# `pdb2reaction fix-altloc`

> Auto-generated by `scripts/generate_reference.py`. Do not edit this file manually.

```text
usage: generate_reference.py [-h] -i INPUT [-o OUT] [--recursive RECURSIVE]
                             [--inplace INPLACE] [--overwrite OVERWRITE]
                             [--force FORCE]

Blank PDB altLoc column (col 17) without shifting, and keep one altLoc per
atom by default rule: highest occupancy, then earliest appearance.

options:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Input PDB file or directory
  -o OUT, --out OUT     Output file (if input is a file) or output directory
                        (if input is a directory).
  --recursive RECURSIVE
                        When input is a directory, process *.pdb recursively
                        (including subdirectories). Use True/False.
  --inplace INPLACE     Overwrite input file(s) in place (creates .bak next to
                        each file). Use True/False.
  --overwrite OVERWRITE
                        Allow overwriting existing output files. Use
                        True/False.
  --force FORCE         Process files even if no altLoc is detected (default:
                        skip files without altLoc). Use True/False.
```
